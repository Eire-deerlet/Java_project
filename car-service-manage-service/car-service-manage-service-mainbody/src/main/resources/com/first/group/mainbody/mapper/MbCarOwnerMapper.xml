<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.first.group.mainbody.mapper.MbCarOwnerMapper">

    <select id="findCarOwner" resultType="com.first.group.mainbody.pojos.MbCarOwner" parameterType="com.first.group.mainbody.dtos.CarOwnerDto">
        select * from mb_car_owner
        <where>
            <if test="dto.name != ''">
                name like concat(concat('%',#{dto.name}),'%')
            </if>
            <if test="dto.phoneNumber != ''">
                phone_number like concat('%',#{dto.phoneNumber},'%')
            </if>
            <if test="dto.status != null">
                status = #{dto.status}
            </if>
        </where>
    </select>

</mapper>