<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.first.group.settlement.mapper.BillsDetailsMapper">

	<select id="findDetailsById" resultType="com.first.group.settlement.pojos.SmBillsDetail">
		select * from sm_order_details sod where sod.bill_id = #{billId}
	</select>

</mapper>